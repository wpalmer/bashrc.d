alias ll='ls -l'
function ..(){
	cd ..
}
function dirs(){
	[ $# -eq 0 ] && builtin dirs -v | tac
	[ $# -gt 0 ] && builtin dirs "$@"
}
function mcd(){
    local args=("$@")
    mkdir "${args[@]}" && cd "${args[${#args[@]}-1]}"
}
